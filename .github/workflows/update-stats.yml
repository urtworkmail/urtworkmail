name: Global Code Stats
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Profile Repo
        uses: actions/checkout@v5

      - name: Install cloc
        run: sudo apt-get install -y cloc

      - name: Fetch and Count All Repos
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir temp_repos && cd temp_repos
          gh repo list urtworkmail --visibility=public --limit 100 --json url -q '.[].url' | xargs -n 1 git clone --depth 1
          cd ..
          
          cloc temp_repos --md --exclude-dir=.github > stats.md
          
          # Using plain text markers to prevent copy-paste bugs
          sed -n '1,/\[CLOC-START\]/p' README.md > README.new
          cat stats.md >> README.new
          sed -n '/\[CLOC-END\]/,$p' README.md >> README.new
          
          mv README.new README.md

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Update total lines across all repos (Fixed Markers)" || exit 0
          git push
